# 貢献の手引き

marche-kit への貢献を歓迎します。このファイルは**何を送ってよくて、何を確認してから
送るか**をまとめたものです。まず [docs/concepts.md](docs/concepts.md) を読んでください。
このプロジェクトの判断は、ほぼすべてそこに書いた3層モデルから導かれます。

Issue と Pull Request は**日本語でも英語でもかまいません。**
ドキュメントは日本語で書かれています（[README.en.md](README.en.md) を除く）。

## このプロジェクトが解こうとしていること

イベント公式サイトの「開催直前の更新」を、運営を通さずに出店者自身が回せるようにすることです。
**承認フローを持たないこと**と、**PHPが動くだけのレンタルサーバーで完結すること**が
設計の前提にあります。この2つは機能ではなく制約です。

## 歓迎する変更

| | 例 |
|---|---|
| テーマ | 新しいテーマ、既存テーマの改善、代替画像やSNSアイコンの追加 |
| 文言辞書 | `core/i18n/` への言語追加、既存の訳の改善 |
| ドキュメント | 誤り・古くなった記述の修正、つまずいた箇所の追記 |
| バグ修正 | 仕様どおりに動かない箇所。**再現手順を添えてください** |
| 検証ツール | `tools/validate.py` が見落としている仕様違反 |

## 送る前に立ち止まってほしい変更

| | なぜか |
|---|---|
| コアにイベント固有のものを入れる | 下の「コアかテーマか」を読んでください |
| 承認フローの追加 | 運営がボトルネックになります（[concepts.md](docs/concepts.md)） |
| ビルド工程・npm依存の導入 | **ファイルを置けば動く**ことが、このプロジェクトの動作要件です（[決定13](docs/decisions.md)で一度検討して却下しています） |
| データベースの導入 | 同上。JSONファイルで運用が回っています |
| 既に却下した案の再提案 | [docs/decisions.md](docs/decisions.md) の「検討したが採らなかった案」を確認してください |

[ロードマップの「意図的にやらないこと」](docs/roadmap.md#意図的にやらないこと)に
理由を一覧にしています。**却下理由がもう有効でないと考えるなら、それを書いてIssueを立ててください。**
前提が変われば決定も変わります。

## コアかテーマか

このプロジェクトで一番よく迷う判断です。境界は一線で引かれています。

> **コアはクラス名とDOMを出力する。見た目は決めない。**

| 迷ったら | 行き先 |
|---|---|
| 色・余白・フォント・角丸・影・アニメーション | テーマ |
| カードをどう並べるか、ナビをどこに固定するか | テーマ |
| 画像（代替画像・SNSアイコン）| テーマ |
| どのJSONをどう読んでDOMにするか | コア |
| 画面に出る文字列 | **どちらでもなく辞書**（`core/i18n/`） |
| 開催日・カテゴリ・対価の単位・言語 | **どちらでもなく設定**（`marche.config.json`） |

判断に使える問いが2つあります。

1. **他のイベントでも同じ答えになるか。** ならなければコアには入りません
2. **カテゴリIDや店舗名が条件に出てくるか。** 出てきたらイベント固有です

コアのコードに `category === 'beer'` のような分岐を書かないでください。
**カードには `variant` と `data-category` の両方が出力されています。**
テーマ側は `variant` で書き、どうしてもというときだけ `[data-category]` を使ってください。

## 手元で動かす

ビルドも依存のインストールもありません。

### サイトの描画だけを見る

```bash
cd examples/demo
python3 -m http.server 8000
```

<http://localhost:8000/?fixed> が開きます。`?fixed` は表示順のシャッフルを止めます。

**テーマを当てたい場合**は、公開ディレクトリの形に組み立てます。
手順は [docs/setup.md](docs/setup.md) の「配置用のディレクトリを組み立てる」にあります。

### PHPとエディタまで動かす

`python3 -m http.server` ではPHPが動かないため、保存の往復を試すには内蔵サーバーを使います。

```bash
cd <組み立てた公開ディレクトリ>
php -S 127.0.0.1:8000
```

`http://127.0.0.1:8000/editor/` が編集画面です。
`tools/inject-env.py` を実行していれば `?<管理キー>` で運営向けUIが出ます。

**PHPを用意せずにエディタの見た目だけ確認する**こともできます。
`core/editor/editor.js` の `UPLOAD_URL` を空文字にすると、送信内容を画面に出すだけになります。

### データを検証する

```bash
python3 tools/validate.py examples/demo
python3 tools/validate.py examples/demo/variants/ticket
python3 tools/validate.py examples/demo/variants/oneday
```

**この3つは常に仕様違反0で通る必要があります。** コアやスキーマを変えたら必ず実行してください。

## 変更の種類ごとに

### テーマを追加する

`themes/<名前>/` を作り、[themes/README.md](themes/README.md) の「テーマを作る」の
8項目を確認してください。特に次の3つが漏れやすいところです。

- [テーマ仕様の「コアが探すスロット」](docs/theme-contract.md)がすべて存在すること
- [「テーマが用意する仕掛け」の7つ](docs/theme-contract.md#テーマが用意する仕掛け)が書かれていること
- **条件によって出力されない要素が無くても崩れないこと**（`price-note`、SNSのスロット、ロゴなど）

`default/` からコピーするなら、`[data-category]` を使ったスタイルが紛れ込んでいないか
確認してください。**そのテーマが他のイベントで使えなくなります。**

同梱のSNSアイコンを足す場合、**各社のロゴにはブランドガイドの条件があります。**
使えるかどうかを確認したうえで送ってください。

### 文言辞書に言語を足す

`core/i18n/<言語コード>.json` を追加します。`ja.json` をコピーして訳してください。
**キーを増減させないこと。** 見つからないキーはキー名がそのまま画面に出ます。

`marche.config.json` の `locale` を変えて、描画・エディタ・PHPのエラーメッセージの
3箇所すべてで新しい辞書が引かれることを確認してください。**日付の表記も言語で変わります。**

### コアを直す

**先に [docs/decisions.md](docs/decisions.md) を読んでください。** 17件の決定があり、
それぞれに却下した案が併記されています。既に検討済みの可能性があります。

コアの挙動を変える修正では、[データ仕様](docs/data-contract.md)と
[テーマ仕様](docs/theme-contract.md)のどちらかを併せて直すことが多いです。
**仕様に書かれていない挙動に依存するテーマを作らせないでください。**

設計の分かれ道になる変更（どちらの選択肢もあり得るもの）は、
`docs/decisions.md` に「現状 → 何が困るか → 決定 → 検討したが採らなかった案」の形で
追記してください。**なぜそうしなかったかの記録が、後から一番効きます。**

### サーバー側（PHP）を直す

**クライアントを信用しない**という前提を崩さないでください。エディタを通さない
直接POSTでも壊れたデータが残らないよう、受け取った値はすべてサーバー側で確かめています。

`core/php/config.php` の3つの定数は `core/editor/editor.js` にも同じ値があります。
**片方だけ変えると、エディタを通ったデータがサーバーで弾かれます。**

| `core/php/config.php` | `core/editor/editor.js` |
|---|---|
| `MARCHE_ALLOWED_EXTENSIONS` | `ALLOWED_EXTENSIONS` |
| `MARCHE_MAX_TOTAL_IMAGE_BYTES` | `MAX_TOTAL_IMAGE_BYTES` |
| `MARCHE_ITEM_STATUSES` | `ITEM_STATUSES` |

画面に出る文字列は直書きせず、`marche_text('server.badShopId')` のように辞書から引きます。
**新しいエラーメッセージを足すときは `ja.json` と `en.json` の両方に足してください。**

### データ仕様を変える

3箇所が同時に動きます。**どれか1つだけを直さないでください。**

| | |
|---|---|
| `schema/*.json` | 形式の正 |
| `docs/data-contract.md` | 人が読む仕様 |
| `tools/validate.py` | 検証の実装 |

サーバー側（`core/php/`）の検証も同じ規則を持っています。

## 送る前のチェックリスト

- [ ] `python3 tools/validate.py` がデモ3構成とも仕様違反0で通る（**スロットとの照合を含む**）
- [ ] デモを**テーマ無しの素の状態**で開いて、描画が壊れていない
- [ ] コアを変えたなら、**同梱の2テーマ**（`default/` と `night-market/`）の両方で見た
- [ ] フォームに触れたなら、**おとり欄（`.courier-honeypot`）が画面の外にある**ことを確かめた
- [ ] 画面に出る文字列を足したなら、`ja.json` と `en.json` の両方に入れた
- [ ] `?fixed` を付けて確認した（付けないと並びが毎回変わり、差分が分かりません）
- [ ] **実在の店舗名・連絡先・画像を入れていない**（デモはすべて架空です）
- [ ] **実運用の値を入れていない**（下記）

## 秘密情報を入れない

**このリポジトリに、実運用の通知先メールアドレス・管理キー・Webhook URLを含めないでください。**

- `.env` は `.gitignore` 済みです。`.env.example` にはプレースホルダだけを置きます
- `core/php/secrets.php` は生成物です。コミットしません
- `core/editor/*.js` にはプレースホルダ（`__ADMIN_KEY__` など）だけを置きます。
  **実際の値は配置後のコピーに対してだけ注入されます**（`tools/inject-env.py`）

うっかり混入させてしまった場合は、**PRを閉じて履歴ごと作り直してください。**
値の無効化（管理キーの変更など）も忘れずに行ってください。

## コミットとPull Request

- **1つのPRで1つのことをしてください。** テーマの追加とコアの修正は分けます
- コミットメッセージは日本語でかまいません。**何を変えたかより、なぜ変えたかを書いてください**
- 挙動を変えるPRには、**手元で何を確認したか**を書いてください。
  このリポジトリのドキュメントは「動作確認したこと」を残す形で書かれています

自動テストはありません。**確認は手で行い、何を見たかを文章で残す**という方針です
（[docs/roadmap.md](docs/roadmap.md) の各段階を見てください）。

## ライセンス

MIT です。Pull Request を送ることで、その変更が MIT で配布されることに同意したものとみなします。
