# marche-kit 紹介ページの原稿

Claude Design に渡すための、掲載文と特徴のまとめです。
このファイル自体は公開しません。ここにあるテキストを素材に、1枚のサービス紹介ページ（LP）を組みます。

想定読者は2種類です。**イベントの運営担当者**（多くはエンジニアではない）と、
それを実際に構築する**開発者**。前者に「何が楽になるか」を先に見せ、
後者が知りたい技術的な条件を下に置く並びにします。

---

## 1. 一言でいうと

> イベント公式サイトの更新を、出店者自身の手に渡す仕組み。

## 2. キャッチコピー案

推しは A です。運営の負担が減ることと、その手段（出店者が直接直す）が1行に入っています。

| | 案 | 補足 |
|---|---|---|
| **A（推奨）** | **出店者が、自分で直す。** | サブコピー: 開催直前の更新を運営が抱え込まないための、イベントサイトの仕組み |
| B | 締切のあとに届く原稿を、待たなくていい。 | 運営の実感に寄せた案。ややネガティブ寄り |
| C | データベースも、管理画面も、SaaSも使わない。 | 開発者向け。技術要件を前面に出す案 |

## 3. リード文（ヒーローの下に置く2〜3文）

イベントの公式サイトで一番手間がかかるのは、作ることではなく開催直前の更新です。
出店者が30店あれば、紹介文と商品情報が30通り集まります。締切を過ぎてから届く原稿、直前の商品変更、当日の完売報告。
marche-kit は、その更新を出店者自身に渡します。運営には通知だけが飛びます。

## 4. 誰のためのものか

クラフトビール祭り、マルシェ、朝市、文化祭、地域イベント。
**「出店者が集まり、それぞれが商品を並べる」形のイベント**の公式サイトを想定しています。

出店者は自分の紹介文・ロゴ・商品・価格・完売状況を、専用の編集画面から直します。
運営はお知らせを足します。来場者は問い合わせフォームから連絡します。
どれもコードを触らず、サイトを作り直すこともなく回ります。

---

## 5. 掲載する特徴

ページの主役はここから6つ。それぞれ見出し1行＋本文2〜3文で置きます。

### 5-1. 出店者が保存した瞬間、サイトに出る

承認フローは意図的に持っていません。運営の確認を挟むと、運営が再びボトルネックになるからです。
出店者は自分の店について当事者であり、間違える動機がありません。
間違いがあれば後から直せばよい、という運用に振り切っています。

> ページ上のひとこと: **待つ人を作らない。**

### 5-2. データベースがいらない

必要なのは、セキュリティサポートが続いているPHPが動く一般的なレンタルサーバーだけです（2026年8月時点で8.2以上）。
管理画面フレームワークも、外部SaaSも使いません。データはすべてサーバー上のJSONファイルとして置かれます。
月額のかかる基盤を増やさずに始められます。

> 補足として小さく: MySQL不要／Node.js不要／ビルド不要／アカウント登録不要

### 5-3. ビルド工程がない

読み込むのはESモジュール1行だけです。npm install も、デプロイパイプラインも要りません。
ファイルをサーバーに置けば動きます。**サイトを引き継いだ人が、5年後に何も分からなくなる状態を避けるための選択です。**

### 5-4. テーマを掛け替えるだけで、別のイベントの見た目になる

marche-kit はマルシェの構造をそのまま設計に借りています。

| 層 | フランス語の意味 | 中身 | 差し替え |
|---|---|---|---|
| **Halle** | 市場の屋根 | 仕組みの本体（PHP・描画JS・編集画面） | しない |
| **Étal** | 各店の陳列台 | 出店者ごとのデータと画像 | イベントごと |
| **Auvent** | 掛け替える日よけの布 | CSS・レイアウト・配色・フォント | 自由 |

境界は一線で引いてあります。**コアはクラス名を出力する。見た目は決めない。**

標準で2つのテーマが入っています。中立な `default` と、暗い配色でナビを画面下に固定した `night-market`。
**同じコア・同じHTML構造のまま、CSSだけでここまで変わります。**

> ページ上では、この2テーマのスクリーンショットを並べて見せるのが一番効きます（→ 12. 素材）。

### 5-5. イベント固有の設定は、1ファイルに集まっている

`marche.config.json` の1枚で、開催日・出店者の区分・商品カテゴリ・対価の単位・表示言語が決まります。
対価をチケット枚数で受け取るイベントも、1日だけの開催も、設定だけで形が変わります（開催が1日なら「販売日」の表示自体が消えます）。
表示文言は日本語と英語の辞書を同梱しています。

### 5-6. 出店者の入力を、そのままHTMLにしない

出店者が入力する文字列はすべてテキストとして扱い、HTMLタグはサーバー側で除去します。
画像は拡張子とサイズを検証し、店舗IDと商品IDの形式を固定して、他店のデータに触れられないようにしています。
問い合わせフォームには確認モーダルとスパム対策（ハニーポット）が入っています。

### そのほか（ページ下部に小さく一覧で）

| | 中身 |
|---|---|
| お知らせの編集 | 運営が追加・修正。サイトの再ビルドは不要 |
| 問い合わせフォーム | 項目定義のJSONから自動で組み立て。確認モーダル・自動再試行・メール送信つき |
| イベント公式のSNS | 設定にURLを並べるとサイトに出る。アイコンはテーマが持つ |
| 検証ツール | `tools/validate.py` がデータの不整合をまとめて報告。**テーマ側の置き忘れも検出する** |
| 多言語 | 画面の文言は日本語・英語を同梱 |
| ライセンス | MIT。リポジトリごとコピーして使うテンプレート方式 |

---

## 6. 提供のしかた — ソフトは無償、手を動かす部分が有償

**marche-kit そのものは MIT ライセンスの無償公開です。** ダウンロードして自分で設置すれば、費用はかかりません。
そのうえで、**設置と運用を代行する有償サービス**を用意しています。

| | OSS版 | 設置・運用サービス |
|---|---|---|
| 費用 | **無償** | **有償**（要見積もり） |
| 入手 | GitHub からコピーして使う | [お問い合わせ](https://crssrds.jp/contact)から |
| 誰がやるか | 自分（または自社の開発者） | こちらで対応 |
| 中身 | ソフトウェア一式・ドキュメント・テーマ2種 | サーバーへの設置、イベントに合わせた設定、テーマ制作、開催期間中のサポート |
| 向いている人 | サーバーとファイル配置が分かる人がいる | 運営に技術担当がいない／開催まで時間がない |

書き方の方針は3つ。

まず、無償であることを先に、はっきり出します。有償サービスを先に見せると、
OSS版に隠れた制限があるのではと読者に疑わせます。**機能差はありません。同じものです。**
違うのは、設置と運用を自分でやるか、こちらがやるかだけです。

次に、**金額は書きません。** イベントの規模も、テーマを新規に作るかどうかも案件ごとに違います。
「要見積もり」と書き、問い合わせフォーム（https://crssrds.jp/contact ）への導線を置きます。

最後に、有償側を売り込みません。OSS版で完結できる人はそれでよく、
そのほうが marche-kit にとっても健全です。有償サービスは「自分で設置するのが難しいときの選択肢」として、
1セクション分の面積で置きます。

> ページ上のひとこと: **ソフトは無償。手を動かす部分だけ、有償です。**

## 7. 前提と注意（隠さず載せる）

**これは信頼できる出店者の範囲で使う仕組みです。**

出店者が保存した内容は、運営の確認を挟まずそのまま公開されます。
そのため marche-kit は、**出店者の身元が分かっていて、連絡が取れる関係にある**イベントを想定しています。
不特定多数が自由に登録して出店するような場では、そのまま使わないでください。

ページの下のほうに、注意書きとして枠で囲んで置きます。折りたたまず、読める形のまま出します。
「使えない場合を先に言う」ことが、この種のツールでは信頼の材料になります。

## 8. 出自（末尾近くに1ブロック）

奈良クラフトビール祭り公式サイト（[naracraft.beer](https://naracraft.beer)）で実際に運用している仕組みを、
他のイベントでも使えるように切り出したものです。**36店・131商品の実データで仕様を検証しています。**
運用して残ったものだけが入っています。

---

## 9. ページ構成案

上から順に。1カラムのスクロール1枚もので、途中に画面写真を2回入れます。

| # | セクション | 中身 |
|---|---|---|
| 1 | ヒーロー | キャッチコピーA ＋ リード文（§3） ＋ ボタン2つ（**GitHubで入手（無償）** / **設置・運用の相談** → https://crssrds.jp/contact ） ＋ 背景に出店者カードのグリッド |
| 2 | 誰のためか | §4。対象イベントの例をアイコン付きで横並び（マルシェ／朝市／ビール祭り／文化祭） |
| 3 | **課題** | 「開催直前、原稿が集まらない」を3コマ程度で。ここが感情の入口 |
| 4 | **特徴** | §5-1〜5-6の6ブロック。2列グリッド、または左右交互のジグザグ |
| 5 | テーマの比較 | §5-4。default と night-market のスクリーンショットを並べ、「同じデータ・同じコア」と添える |
| 6 | 3層モデル | Halle / Étal / Auvent の図。§5-4の表を図解に起こす |
| 7 | 動作要件 | PHP・DB不要・ビルド不要を、チェックリストのような形で短く |
| 8 | 前提と注意 | §7。枠囲み。他と色を変える。**有償サービスの話より前に置きます** |
| 9 | 提供のしかた | §6。無償と有償の2列で並べる。金額は書かず「要見積もり」 |
| 10 | 始め方 | `git clone` の3行 ＋ ドキュメントへのリンク（setup.md / concepts.md）＋ 相談フォーム（https://crssrds.jp/contact ）への導線 |
| 11 | 出自とライセンス | §8 ＋ MIT ＋ GitHubリンク |

**セクション3（課題）とセクション4（特徴）が本体です。** 他は薄くてかまいません。
セクション9（提供のしかた）は、売り込みの面積を取りすぎないよう1画面に収めます。

## 10. トーン・配色の方針

**落ち着いた道具の顔をさせます。** 派手なグラデーションや、動きの多いアニメーションは使いません。
「イベントのサイト」ではなく「イベントのサイトを作る道具」の紹介ページです。にぎやかにするとイベント本体と混ざります。

配色は、標準テーマ `themes/default/tokens.css` の値をそのまま基調にすると、
ページと実物の見た目が地続きになります。

| 役割 | 値 |
|---|---|
| プライマリ | `#2f6f4e`（深い緑） |
| プライマリ・濃 | `#24573d` |
| プライマリ・淡 | `#e6f1ea`（セクション背景に） |
| 本文 | `#23282d` |
| 補助テキスト | `#6b7280` |
| 背景 | `#ffffff` / 沈んだ面 `#f4f5f6` |
| 罫線 | `#dcdfe3` |

- 書体は日本語のゴシック（Noto Sans JP など）。**見出しも本文も同じ書体で、太さで差をつけます。**
- 角丸は控えめ（カード12px、ボタン4px）。影も弱く。
- 幅は1100pxまで。スマートフォンでの表示を前提にします（運営も出店者も、当日は現場でスマホを見ます）。
- ダークモード対応は任意。やるなら `night-market` テーマの配色を借ります。

## 11. 書かないこと

- **競合との比較**（WordPress、STUDIO、Notion など）。優劣を主張する文書にしません
- **「簡単」「誰でも」「たった3分で」**。配置にはサーバーの知識が要ります。過大な約束をしません
- **導入実績の数字を実際より大きく見せる表現。** 実運用しているのは現時点で1サイトです
- 承認フローがないことを、機能不足として謝る書き方。**これは選んだ設計です**（§5-1）
- **具体的な金額・料金プラン・「初期費用〇〇円〜」。** 案件ごとに違うため、決まっていない数字を出しません（§6）
- OSS版を機能制限版のように見せる書き方。**無償版と有償サービスで、ソフトウェアは同じものです**

## 12. 素材とリンク

| | |
|---|---|
| リポジトリ | https://github.com/kwaka1208/marche-kit |
| ライセンス | MIT |
| スクリーンショット | `examples/demo` を起動し、`themes/default` と `themes/night-market` を当てて撮る |
| 図 | 3層モデル（README.md と docs/concepts.md に元がある） |
| 詳しい説明 | docs/concepts.md（設計思想）／docs/setup.md（配置手順） |
| 有償サービスの問い合わせ先 | https://crssrds.jp/contact |
