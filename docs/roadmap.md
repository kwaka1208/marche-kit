# ロードマップ

## 段階1 — 契約の確定 ✅ 完了

コードを1行も動かす前に、コアとテーマの境界を文章で決めました。
抽出を始めてから「これはコアかテーマか」で迷わないためです。

- [x] リポジトリの骨格
- [x] [設計思想と3層モデル](concepts.md)
- [x] [データ契約](data-contract.md) / [テーマ契約](theme-contract.md)
- [x] JSONスキーマ（`schema/` 5件）
- [x] 検証ツール（`tools/validate.py`）
- [x] 本家の実データ（36店 / 131商品）と突き合わせて契約を検証
- [x] [設計上の決定](decisions.md) — 一般化の論点7件に決着

## 段階2 — コアの移植（PHP・エディタ） ✅ 完了

naracraft.beer から仕組み部分を抽出し、イベント固有の記述を外しました。

- [x] `core/php/` — 出店者情報の受信・保存、お知らせ書き込み、メール送信
- [x] `core/editor/` — 出店者エディタ、お知らせエディタ
- [x] 設定を `marche.config.json` に集約（[決定7](decisions.md)）
- [x] **語彙の改名**（`shop` / `item`）— [決定3](decisions.md)
- [x] **HTMLタグ除去の検証を追加**（[決定6](decisions.md)）
- [x] `saleDays` を配列化し、`days` と照合（[決定2](decisions.md)）
- [x] 通知先・管理キー・Webhook URLを環境変数から注入する仕組み（`tools/inject-env.py`）
- [x] エディタの表示文言も辞書に外出し（[決定4](decisions.md)）
- [x] 商品カテゴリを設定で定義（[決定8](decisions.md)）
- [x] 販売日はチェックボックス（[決定9](decisions.md)）
- [x] ロスター外の店舗IDからの保存を拒否（[決定10](decisions.md)）

**このリポジトリに実運用の通知先メールアドレスやWebhook URLを含めないこと。**
`.env.example` にプレースホルダだけを置いています。

### 動作確認したこと

PHPの内蔵サーバーに架空のイベントを立てて確認しました。

- 出店者エディタでの読み込み・編集・送信・保存の往復（新規商品のID採番を含む）
- お知らせエディタでの読み込み・編集・送信・保存の往復
- HTMLタグの除去、パストラバーサル・他店ID詐称・拡張子偽装の拒否
- 1日開催で販売日UIが消えること、チケット運用で整数のみ受け付けること
- 商品カテゴリ未定義のときカテゴリ欄が出ないこと

## 段階3 — 描画JSの分解 ← 次

現行の描画JSを、コアとテーマに切り分けます。

- [ ] 描画ロジック（データ取得、カード生成、ポップアップ、フィルタ、シャッフル）を `core/js/` へ
- [ ] クラス名とCSSを `themes/default/` へ
- [ ] カテゴリ固定の解消 — 器を `data-marche-shops` に、カードに `variant` と `data-category`（[決定1](decisions.md)）
- [ ] 対価表示を `price-value` / `price-unit` / `price-note` に分割（[決定5](decisions.md)）
- [ ] 表示文言を `core/i18n/` の辞書から引く（[決定4](decisions.md)）
- [ ] 開催1日のとき販売日UIを出力しない（[決定2](decisions.md)）

語彙の改名は段階2で完了しています（`shop` / `item`）。
テーマ契約のクラス名は、この段階で実際に出力されるようになります。

## 段階4 — 動く最小サンプル

- [ ] `examples/demo/` — 架空のイベントで、テーマなしの素の状態から組み立てる
- [ ] 出店者3店、カテゴリ2種、お知らせ2件程度のダミーデータ
- [ ] チケット運用（`pricing.mode: "ticket"`）でも動くことを確認
- [ ] 1日開催で販売日UIが消えることを確認

**ここが分離できているかの実質的なテストです。**
デモが素の状態で動かないなら、コアにテーマが混ざっています。

## 段階5 — 2つ目のテーマ

- [ ] `themes/` にもう1つ、意図的に雰囲気の違うテーマを作る
- [ ] カテゴリIDに依存せず `variant` だけで書けるか確認

テーマが1つしかないうちは、分離できているつもりでも必ずどこかが癒着しています。
2つ目を作って初めて契約の穴が見つかります。

## 段階6 — 本家サイトの移行

- [ ] naracraft.beer を marche-kit ベースに寄せる
- [ ] データ移行（`menus` → `items`、`saleDays` の配列化、`<br>` → `\n`、`tickets` の除去）
- [ ] 現行サイトとの表示差分を確認
- [ ] 出店者エディタの動作確認（**実運用中のため慎重に**）

急ぎません。marche-kit 側が安定してから着手します。

## 段階7 — 公開の準備

- [ ] README の英語版
- [ ] セットアップ手順（レンタルサーバーへの配置、パーミッション、動作確認）
- [ ] CONTRIBUTING.md

## 意図的にやらないこと

| やらないこと | 理由 |
|---|---|
| npmパッケージ化 | PHPを含み、カスタマイズ前提。テンプレート方式で足りる |
| 管理画面フレームワークの導入 | レンタルサーバーで動く軽さが利点 |
| データベース | 同上。JSONファイルで運用が回っている |
| 承認フロー | 運営がボトルネックになる（[concepts.md](concepts.md)） |
| グリッドの静的化 | 表示順シャッフルの公平性を優先（[concepts.md](concepts.md)） |
| お知らせのクライアントキャッシュ | 古い情報を見せない方を優先（[concepts.md](concepts.md)） |
| チケットと現金の併用表示 | 実運用に前例がなく、データ構造が複雑になる（[決定5](decisions.md)） |
| i18nライブラリ・言語切り替えUI | 辞書1枚で足りる（[決定4](decisions.md)） |
| 旧形式の読み込み互換 | 切り捨てて構造を単純に保つ（[決定1・2](decisions.md)） |
